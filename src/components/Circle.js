import React, { PropTypes } from 'react';

const Circle = ({}) => {
  return (
    <div style={styles}>
    </div>
    );
}

const styles = {
  width: '100px',
  height: '100px',
  backgroundColor: '#DDDDDD',
  border: '1px solid black',
  borderRadius: '50%',
  position: 'relative'
}

export default Circle;
